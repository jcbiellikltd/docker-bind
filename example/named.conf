options {
	directory "/var/lib/bind";

	listen-on { any; };
	listen-on-v6 { any; };

	allow-transfer {
		none;
	};

	pid-file "/var/run/named/named.pid";

	allow-recursion { none; };
	recursion no;
};

zone "example.com" IN {
	type master;
	file "example.com.zone";
};
